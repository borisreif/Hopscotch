{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build demo",
      "type": "shell",
      "command": "mkdir -p build && g++ -std=c++20 -Wall -Wextra -pedantic -g -O0 demo.cpp HopscotchMap.cpp -o build/demo",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$gcc"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "Build the HopscotchMap demo with g++ in debug mode."
    },
    {
      "label": "run demo",
      "type": "shell",
      "command": "./build/demo",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "dependsOn": "build demo",
      "problemMatcher": [],
      "group": "test",
      "detail": "Build and run the demo executable."
    }
  ]
}
